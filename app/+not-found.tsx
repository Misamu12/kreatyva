import { StyleSheet, Text, View } from "react-native";

export default function notfound() {
  
  <View>
    <Text style={styles.bigT} >404</Text>
  </View>;
};

const styles = StyleSheet.create({
  bigT : {
    color : 'red'
  }
});
